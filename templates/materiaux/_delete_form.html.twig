<form method="post" action="{{ path('materiaux_delete', {'materiauxId': materiaux.materiauxId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiaux.materiauxId) }}">
    <button class="btn">Delete</button>
</form>
