 <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

{{ form_start(form) }}
<div class="formProduit grille"> 
    
    <div class="formAttribut grille"> 
        <div class="formulaire nomProduit">
        {{ form_row(form.titre) }}
        </div>

        <div class="formulaire description">
        {{ form_row(form.description) }}
        </div>

    </div>

    <div class="formImage grille"> 
        <div id="import" class="formulaire image ">
        <i class="fa-solid fa-file-arrow-up"></i>{{ form_row(form.image) }}
        </div>

   {# verifier la route pour "edit" #}

    {% if app.request.attributes.get('_route') == 'projet_edit' %}
        <div class="imageProduit">
        {% for image in projet.image %}
            <div class="imageIndividuelle">
                <img  class="images_projet" src="{{ asset('/images/images_produit/' ~ image.nom) }}" alt="Image" width="150">
                
                            </div>                          
        {% endfor %}
        
    {% endif %}
        </div>     
    </div>
</div>    
    <button class="botnform">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}


